Para probar el codigo instalar las dependencias usando  
```pip install -r requirements.txt```
luego ejecutar el archivo Protocompile.sh  
```./ProtoCompile.sh```
y por ultimo correr el servidor y luego el cliente  
```python server.py```  
```python client.py```
