syntax = "proto3";

import "google/protobuf/empty.proto";

service DataService {
    rpc EnviarParticion(NombreChunk) returns (Particion) {}
    rpc RecibirParticion(Particion) returns (google.protobuf.Empty) {}
}

message NombreChunk {
    string nombre = 1;
}

message Particion {
    bytes contenido = 1;
    string fileInfo = 2;
}